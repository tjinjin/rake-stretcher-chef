require 'spec_helper'

describe Rake::Stretcher::Chef do
  it 'has a version number' do
    expect(Rake::Stretcher::Chef::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
